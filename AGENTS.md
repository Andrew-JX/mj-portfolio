# mj-portfolio · 协作规则

> 最后复核：2026-08-08 · 超过 90 天或 5 个批次未复核，第④步不得跳过，必须逐条对照当前仓库事实重新确认。

## 事实边界
- 这是一个公开的个人作品集网站，所有页面内容对外可见。
- 页面文案是给访客看的产品文案。不要把交互说明、开发过程、任务要求或与我的对话内容写进页面。
- 页面上不出现任何公司项目、客户名称、业务数据或与在职工作相关的具体内容。

## 目录清单

<!-- machine-checked: architecture-manifest -->

| 目录 | 作用 |
|---|---|
| `src/assets` | 构建时引入的静态资源 |
| `src/components` | 可复用展示组件 |
| `src/composables` | 复用 hooks |
| `src/data` | 页面数据源，只放数据不放逻辑 |
| `src/pages` | 路由页面组件 |
| `src/router` | 路由表 |
| `src/styles` | 全局样式 |
| `docs` | 长文档 |
| `nginx` | 生产 nginx 配置的参考副本，服务器上那份需手工同步 |
| `ops` | 服务器端发布脚本，需手工安装到服务器，仓库副本不会自动生效 |
| `public` | 原样复制到产物根目录的静态文件 |
| `scripts` | 仓库自身的门禁脚本 |

新增或删除目录时同步这张表，`npm run check:docs` 会双向比对。

## 交付规则
- 机器验证命令是 `npm run build`（tsc -b && vite build）和 `npm run check:docs`。仓库没有 lint 和单元测试。
- 构建通过不等于页面正确。视觉与交互必须由我人工确认，执行方不得代为判定。
- 每条完成声明只用「达成 / 未达成 / 未验证」，并给出对应证据。
- 不自动 merge、push、tag 或发布。

## 文档规则
- 写成「必须」的规则，要么有机器门禁守着，要么改写成带边界的建议。没有门禁的「必须」等于没写。
- 不写镜像当前实现的快照文档。当前状态由代码和门禁回答；文档只写仍然生效的约束，和无法从代码恢复的决策理由。
- 规则类文档被质疑时要给出验证路径——写清「不信就跑哪条命令」，而不是要求被相信。

## 页面质量硬规则
- 整个页面只有一个滚动条，不允许出现内层滚动容器。
- 动效不得增加阅读成本：需要横向比较的列表，不做整行放大、镜面高光一类效果。
- 尊重 `prefers-reduced-motion`。
- 可交互元素必须是真正的可聚焦元素，能 Tab、能回车触发。
